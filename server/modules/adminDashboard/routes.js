/**
 * modules/adminDashboard/routes.js
 *
 * TODO: V2 — Admin Dashboard module routes.
 * This module is not implemented in the current prototype.
 *
 * Planned endpoints:
 *   GET  /api/admin/users          — List all users
 *   GET  /api/admin/reports        — View all validation reports
 *   GET  /api/admin/metrics        — Platform-wide analytics
 *   PATCH /api/admin/users/:id     — Update user role / ban user
 *   DELETE /api/admin/reports/:id  — Remove a report
 */

// module.exports = router;
